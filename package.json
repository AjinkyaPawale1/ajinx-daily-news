{
  "name": "ajinx-daily-news",
  "private": true,
  "type": "module",
  "description": "Daily news digest, refreshed automatically via a scheduled GitHub Action and OpenAI web search.",
  "scripts": {
    "fetch-news": "node scripts/fetch-news.mjs",
    "fetch-news:mock": "node scripts/fetch-news.mjs --mock",
    "test": "node --test"
  },
  "dependencies": {
    "openai": "^6.45.0"
  },
  "devDependencies": {
    "jsdom": "^29.1.1"
  }
}
